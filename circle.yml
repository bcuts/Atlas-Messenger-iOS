machine:
  xcode:
    version: 8.2
dependencies:
  override:
    - gem install bundler
    - bundle install
test:
  override:
    - bundle exec fastlane test
deployment:
  branch: jpmcglone/deploy
    commands: 
      - bundle exec fastlane deploy
