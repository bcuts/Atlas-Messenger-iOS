machine:
  xcode:
    version: 8.2
dependencies:
  override:
    - gem install bundler
    - bundle install
test:
  override:
    - bundle exec fastlane test
deployment:
  internal_distribution:
    branch: jpmcglone/deploy
      commands: 
        - bundle exec fastlane deploy
